mixin list(data)
  ul
    each item in contents[data].metadata.items
      li
        if item.url
          a(href= item.url)
            h4= item.title
        else
          h4= item.title
        ul
          if item.authors
            li(style="font-style:italic")= item.authors
          li= item.details